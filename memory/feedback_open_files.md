---
name: Open files in VS Code
description: User wants files opened in VS Code, not Safari or other apps
type: feedback
---

Always open files using `code` (VS Code) rather than `open -a Safari` or other applications.

**Why:** User prefers VS Code as their viewer.

**How to apply:** Use `code <filepath>` whenever opening a file for the user to review.
